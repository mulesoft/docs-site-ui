product: dev-docs
component: site-ui
strategy: docker
dockerBuildEnableBuildKit: true
dockerArgs:
  secret:
    GH_TOKEN: GH_TOKEN
    SECRET_KEY: SECRET_KEY
requires:
  credentials:
    - id: ms-cx-engineering-gpg-private-key
      value: SECRET_KEY
    - id: GH_TOKEN
      value: GH_TOKEN