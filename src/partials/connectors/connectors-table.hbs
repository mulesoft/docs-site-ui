<section class="connectors-table-wrapper">
  <table class="connectors-table">
    {{> connectors-search}}
    <tbody>
      {{!-- just mocking things up --}}
      {{> connectors/connectors-tablerow}}
      {{> connectors/connectors-tablerow}}
      {{> connectors/connectors-tablerow}}
    </tbody>
  </table>
</section>
