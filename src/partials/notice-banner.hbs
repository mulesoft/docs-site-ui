{{#if page.attributes.notice-banner-message}}
<div class="notice-banner">
  <p>{{page.attributes.notice-banner-message}}</p>
</div>
{{else}}
  {{#if (or page.componentVersion.asciidoc.attributes.noLongerUpdated page.componentVersion.asciidoc.attributes.olderVersion)}}
  <div class="notice-banner">
    <p>
      {{#if page.componentVersion.asciidoc.attributes.noLongerUpdated}}
      We are no longer updating this version of the documentation.
      {{else}}
      {{#if page.componentVersion.asciidoc.attributes.olderVersion}}
      You’re viewing an older version of the documentation.
      {{/if}}
    {{/if}}
      Switch to the
      <a 
        class="notice-banner-link"
        href="{{site.url}}{{page.latest.url}}">latest version</a>, 
      or use the version selector in the left navigation.</p>
  </div>
  {{/if}}
{{/if}}